#include <stdio.h>

int main() {
    int n, sum = 0, r, temp;
    scanf("%d", &n);
    temp = n;
    while(temp) {
        r = temp % 10;
        sum += r * r * r;
        temp /= 10;
    }
    printf(sum == n ? "Armstrong\n" : "Not Armstrong\n");
    return 0;
}
