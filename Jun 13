#include <stdio.h>

int main() {
    int n, rev = 0, r;
    scanf("%d", &n);
    while(n) {
        r = n % 10;
        rev = rev * 10 + r;
        n /= 10;
    }
    printf("%d\n", rev);
    return 0;
}
